name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. جلب الكود من المستودع
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. البحث عن أي ملف zip وفكه تلقائياً
      - name: Decompress Project File (ZIP)
        run: |
          echo "=== جاري البحث عن ملفات مضغوطة وفكها ==="
          ZIP_FILE=$(ls *.zip 2>/dev/null | head -n 1)
          if [ -z "$ZIP_FILE" ]; then
            echo "خطأ: لم يتم العثور على ملف مضغوط!"
            exit 1
          fi
          unzip -q "$ZIP_FILE" -d extracted_project
          echo "=== تم فك الضغط بنجاح ==="

      # 3. إعداد الجافا (إصدار 11 متوافق جداً مع فلاتر 2)
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '11'

      # 4. إعداد الفلاتر (إصدار قديم متوافق مع مشروعك وحل مشكلة الـ embedding)
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '2.10.5' # استخدام هذا الإصدار يحل مشكلة الـ v1 embedding كلياً

      # 5. بناء التطبيق
      - name: Build APK
        run: |
          cd extracted_project
          if [ -d "neural_craft_flutter" ]; then cd neural_craft_flutter; fi
          
          echo "=== جاري تحميل الحزم متوافقة مع الإصدار ==="
          flutter pub get
          
          echo "=== جاري بناء التطبيق الآن ==="
          flutter build apk --release

      # 6. استخراج ملف الـ APK للتحميل
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: NeuralCraft-APK
          path: |
            extracted_project/build/app/outputs/flutter-apk/app-release.apk
            extracted_project/*/build/app/outputs/flutter-apk/app-release.apk
            
